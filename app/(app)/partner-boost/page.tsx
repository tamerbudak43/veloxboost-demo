import { BoostView } from '@/components/velox/network/boost-view'

export const metadata = {
  title: 'Partner Boost — VELOX',
}

export default function PartnerBoostPage() {
  return <BoostView />
}
